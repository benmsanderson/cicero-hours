{
  "name": "cicero-hours-board-tests",
  "private": true,
  "description": "Dev-only: jsdom harness for the allocation board's browser code.",
  "scripts": {
    "test:board": "python3 tests/build_fixture_dashboard.py /tmp/fixture.html && node tests/board_check.js /tmp/fixture.html",
    "test:cross": "python3 -c \"from tests.synthetic import write_export; from pathlib import Path; write_export(Path('/tmp/cicero_synth.csv'))\" && node tests/cross_check.js /tmp/cicero_synth.csv"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
